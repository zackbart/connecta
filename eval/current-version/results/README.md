# Generated results

`latest.json` and `latest.md` are local benchmark outputs and are ignored. Commit a result only when it is durable release or issue evidence, and link it from the decision it supports.

`issue-350-evidence.md` is retained historical evidence because current provider documentation links to it.
